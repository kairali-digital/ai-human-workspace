# ROLE TEMPLATE

| Field | Value |
|---|---|
| Company | NOT SET |
| Employee or specialist | NOT SET |
| Role | NOT SET |
| One bounded mission source | NOT SET |
| Owned work | NOT SET |
| Approved read-only sources | NOT SET |
| Approved tools | NOT SET |
| Required exit evidence | NOT SET |
| Explicit exclusions | NOT SET |

This role owns its own worker state. It never writes another employee's state and never
inherits another company's facts or permissions by default.
